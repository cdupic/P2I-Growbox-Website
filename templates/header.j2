<nav id="navbar">
    <div>
        <img src="{{ url_for('static', filename='img/icon.png') }}" alt="logo" width="50">
    </div>

    <ul id="navbar-items">
        <li class="navbar-item login-logout">
            {% if authenticated %}
                <a href="{{ url_for('logout_manager') }}">Se déconnecter</a>
                <p> Bonjour, {{ session['user_name'] }} </p>
            {% else %}
                <a href="{{ url_for('login_page') }}">S'identifier</a>
            {% endif %}
        </li>
    </ul>
</nav>

<div id="container">
    {% block content %}{% endblock %}
</div>
