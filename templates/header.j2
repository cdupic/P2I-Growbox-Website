<div>
    <a href="{{ url_for('landing_page') }}">
        <img src="{{ url_for('static', filename='img/icon.svg') }}" alt="GrowBox's icon" />
    </a>
</div>

<div></div>
<div></div>

{% set sidebar_visible = current_sidebar_item != None %}

<nav>
    <ul>
        {% if 'user_name' in session %}
            <li>
                <a href="{{ url_for('greenhouses_page') }}">Mes serres</a>
            </li>
            {% if sidebar_visible %}
                <li>
                    <a href="{{ url_for('logout_manager') }}">Se déconnecter</a>
                </li>
            {% endif %}
        {% else %}
            <li>
                <a href="{{ url_for('login_page') }}">Connexion</a>
            </li>
            <li>
                <a href="{{ url_for('signup_page') }}">Créer un compte</a>
            </li>
        {% endif %}
    </ul>
</nav>
